# Leave this blank when the built frontend is served by the Mirror API in
# production. During local development use http://127.0.0.1:5000.
VITE_API_URL=http://127.0.0.1:5000
# Match PUBLIC_WEBSITE_DOMAIN after the app is hosted with wildcard subdomains.
VITE_PUBLIC_WEBSITE_DOMAIN=
